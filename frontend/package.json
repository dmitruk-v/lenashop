{
  "name": "lenashop",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "esbuild": "esbuild",
    "build": "esbuild ./src/app.jsx --bundle --outfile=../static/js/app.js --minify",
    "watch": "esbuild ./src/app.jsx --bundle --outfile=../static/js/app.js --watch"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "esbuild": "^0.13.4"
  },
  "dependencies": {
    "react": "^17.0.2",
    "react-dom": "^17.0.2"
  }
}